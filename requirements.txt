# Python deps (Streamlit Community Cloud = Linux; keep install reliable)
xgboost
scikit-learn
pandas
numpy
matplotlib
seaborn
streamlit>=1.38.0
joblib
plotly
imbalanced-learn
fpdf2
# scapy: optional (packet sniffing). Installs separately if needed: pip install scapy
# Cloud builds often fail on scapy; app uses simulation without it.
